import AthenaMesh from '../src/index'
import { meshConfig } from './mocks/meshConfig'

describe('@athena-ql/mesh', () => {

  test('the data is peanut butter', async () => {
    // const mesh = new AthenaMesh()
    // await mesh.generateSchema({ config: meshConfig })
    // const ctx = mesh.getContext()
    // console.log(ctx)
  });
});
